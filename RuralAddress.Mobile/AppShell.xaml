<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RuralAddress.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:RuralAddress.Mobile.Views"
    Title="RuralAddress Mobile">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="Login" />

    <ShellContent
        Title="Pânico"
        ContentTemplate="{DataTemplate views:PanicPage}"
        Route="Panic" />

</Shell>
